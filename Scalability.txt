@media screen and (min-width: 768px) {
    body {
        background-color: #e8f4fd; /* Juste pour voir le changement de style */
    }

    .container {
        /* On passe en mode ligne (row) et on permet le retour à la ligne */
        flex-direction: row;
        flex-wrap: wrap;
    }

    .card {
        /* Chaque carte prend environ 48% de la largeur (2 colonnes) */
        flex: 1 1 calc(50% - 10px); 
        background-color: #2ecc71;
    }
}
Patrick Jerier
18:56
@media screen and (min-width: 1024px) {
    body {
        background-color: #fef9e7; /* Juste pour voir le changement de style */
    }

    .container {
        /* On s'assure que tout reste sur une seule ligne */
        flex-wrap: nowrap;
    }

    .card {
        /* Chaque carte prend un tiers de l'espace (3 colonnes) */
        flex: 1;
        background-color: #e67e22;
    }
}
